#include "main.h"

/**
 * _isdigit - checks if a character is digit
 * @c: the carrier variable
 * Return: 1 if this is true, 0 if it is false
 */

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
